import React from 'react';
import './Cart.scss';

const Cart = (props) => {
  return (
    <div className='cart-icon-container'>
      <i className="fas fa-shopping-cart"></i>
    </div>
    );
};

export default Cart;
